[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyvault"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pyvault = "pyvault.main:main"

[tool.setuptools.packages.find]
include = ["pyvault*"]

